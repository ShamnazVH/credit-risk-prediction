# Create environment with conda (handles dependencies better)
conda create -n credit_risk_fixed python=3.10 -y
conda activate credit_risk_fixed

# Install packages using conda
conda install numpy=1.24.3 scikit-learn=1.3.0 pandas=2.0.3 -y
conda install tensorflow=2.13.0 -y
pip install xgboost==1.7.6
pip install flask==2.3.3
pip install joblib==1.3.2


conda install xgboost==1.7.6  
conda install flask==2.3.3
conda install joblib==1.3.2     
